@inherits DarimarSystemComponentWithStyle

@if (GetCurrentHref() == Href)
{
    <MudButton Href="@Href" Class="@Class" Variant="Variant.Text" Color="Color.Primary" Style="@("text-transform:none;" + Style)">@ChildContent</MudButton>
}
else
{
    <MudButton Href="@Href" Class="@Class" Variant="Variant.Text" Style="@("text-transform:none;" + Style)">@ChildContent</MudButton>
}
